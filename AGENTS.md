# Codex Instructions — Cloudwave-Lab

## 언어

- 사용자 대상 응답은 한국어로 작성한다.

## 저장소 성격

- 관측 가능성·AIOps 학습용 실습 킷 모음이다.
- 각 하위 폴더(예: `Bottleneck-Lab/`)는 하나의 독립적인 실습이다.
- 학습자가 관측 데이터로 병목과 이상 위치를 직접 판단해야 한다.
- README 등 실습 문서에 정답을 노출하지 않는다. 시나리오, 지표, 로그, 탐색 방향만 제공한다.

## 작업 원칙

- 요청을 충족하는 최소 범위만 변경한다.
- 추측성 기능, 설정, 추상화를 추가하지 않는다.
- 기존 코드와 문서의 스타일을 따른다.
- 변경한 모든 줄은 요청과 직접 관련되어야 한다.
- 목표, 범위, 완료 기준이 불명확하고 결과에 큰 영향을 주면 사용자에게 확인한다.

## Git 규칙

Git 작업 전에는 반드시 `docs/git-convention.md`를 읽고 최신 규칙을 따른다.

### 브랜치

- `main`은 공유 가능한 기준 소스다.
- 일반 작업은 `feat/`, `fix/`, `docs/`, `refactor/`, `setting/`, `chore/` 접두사의 작업 브랜치에서 수행한다.
- `main`에 직접 push하지 않는다. 작업 브랜치에서 개발한 뒤 PR로 `main`에 머지한다.
- Squash merge를 사용하지 않으며, 머지 후 원격 작업 브랜치를 삭제한다.

### 커밋

- 커밋 메시지는 `[Type] 변경 요약` 형식을 사용한다.
- 제목은 간결체로 50자 이내이며 마침표를 붙이지 않는다.
- 이슈가 있으면 `[Type] #nn - 변경 요약` 형식을 사용한다.
- Type은 `Feat`, `Fix`, `Docs`, `Refactor`, `Setting`, `Chore`, `Merge` 중에서 고른다.
- 커밋과 push는 사용자가 명시적으로 승인한 경우에만 수행한다.

### PR

- PR 제목은 `[Type] 작업 설명` 형식을 사용하고 이슈키는 넣지 않는다.
- 본문은 무엇을 왜 변경했는지 두괄식으로 작성한다.
- 연결할 이슈가 있으면 본문 끝에 `Resolved: #nn`을 적는다.

### 제외 파일

- `.omc/`, `.DS_Store` 등 세션, 도구, 운영체제 상태 파일을 커밋하지 않는다.
