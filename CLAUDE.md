# Claude Code Instructions — Cloudwave-Lab

## 언어
- 사용자 대상 응답은 **한국어**로 작성한다.

## 저장소 성격
- 관측 가능성·AIOps 학습용 **실습 킷 모음**이다. 각 하위 폴더(예: `Bottleneck-Lab/`)가 하나의 실습이다.
- 이 저장소의 킷은 "정답(병목·이상의 위치)을 스스로 찾는" 훈련용이다.
  - **킷 문서(README 등)에 정답을 적지 않는다.** 지표·로그·시나리오만 제공하고, 원인은 학습자가 판단한다.
  - 킷을 수정할 때 이 원칙을 깨지 않는지 확인한다.

## 작업 전 확인
- Git 브랜치·커밋·PR 작업은 `docs/git-convention.md`를 따른다.
- **커밋·push는 사용자 명시 승인 후에만.** `main`에 직접 push 금지.
- `.omc/`, `.DS_Store` 등 세션·툴 상태는 커밋하지 않는다.

## 작업 원칙
- **최소 변경**: 요청을 충족하는 가장 작은 구현. 추측성 추상화·설정·기능 추가 금지.
- **수술적 변경**: 바뀐 줄은 모두 요청과 직접 연결되게. 주변 스타일에 맞춘다.
- **간결성**: 군더더기·상투어·과한 헤지 없이 직접적으로. 기술적 정확성 우선.
- 목표·범위·완료 기준이 불명확하면 "적당한 기본값"으로 채우지 말고 되묻는다.
